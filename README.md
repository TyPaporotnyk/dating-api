# Dating API

A pet project that implements a backend API for a dating application.

The project is designed as a REST API and can be easily run using Docker.

## Tech Stack
- Python
- FastAPI
- PostgreSQL
- Docker / Docker Compose

## Getting Started

### Run with Docker
```bash
docker compose up --build
```
